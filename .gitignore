# Terraform
.terraform/
*.tfstate
*.tfstate.backup
*.tfstate.*.backup
.terraform.lock.hcl
crash.log
*.tfvars

# Lambda layers y builds (generados, no código fuente)
*.zip
lambdas/layers/*/python/

# Python
__pycache__/
*.pyc

# Pruebas locales de invocación (Lambda)
body.json
event.json
out.json
output.json

# Documentos de prueba (pueden tener datos reales)
*.pdf

# Otros
Thumbs.db